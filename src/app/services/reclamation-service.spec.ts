import { ReclamationService } from './reclamation-service';

describe('ReclamationService', () => {
  it('should create an instance', () => {
    expect(new ReclamationService()).toBeTruthy();
  });
});
