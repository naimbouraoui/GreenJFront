import { ActivityService } from './activity-service';

describe('ActivityService', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ActivityService()).toBeTruthy();
  });
});
